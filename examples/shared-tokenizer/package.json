{
  "name": "example-shared-tokenizer",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-package example: one injected exact-tokenizer adapter shared by token-chunk and chat-fit, plus a custom price override for usage-tab.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "start": "tsx index.ts"
  },
  "devDependencies": {
    "token-chunk": "workspace:*",
    "chat-fit": "workspace:*",
    "usage-tab": "workspace:*"
  }
}
