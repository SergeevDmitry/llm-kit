{
  "name": "@llm-kit/model-registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Normalized model metadata and pricing periods used by usage-tab. Bundled into public packages, never published.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "bench": "vitest bench --run"
  },
  "devDependencies": {
    "@llm-kit/test-utils": "workspace:*"
  }
}
