{
  "name": "@llm-kit/tokenizer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Token-counting contracts and the default approximate tokenizer shared by token-chunk and chat-fit. Bundled into public packages, never published.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "bench": "vitest bench --run"
  },
  "devDependencies": {
    "@llm-kit/test-utils": "workspace:*"
  }
}
